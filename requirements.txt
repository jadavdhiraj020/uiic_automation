# ── GUI & Browser Automation ──
PyQt6>=6.5.0
playwright>=1.40.0

# ── Excel Processing ──
xlrd>=2.0.1
openpyxl>=3.1.0

# ── CAPTCHA OCR Engine (PaddleOCR v2 stable branch) ──
# CRITICAL: paddlepaddle must stay < 3.0.0
# Version 3.x has architecture bugs on Windows Intel CPUs
# (onednn_instruction.cc:118). Locked to stable 2.x branch.
paddlepaddle>=2.6.0,<3.0.0
paddleocr>=2.8.0,<3.0.0

# ── Image Processing (PaddleOCR transitive deps) ──
Pillow>=10.0.0
numpy<2.0.0
